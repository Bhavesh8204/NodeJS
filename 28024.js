const vovelslist = ['a','e','i','o','u']

const result = vovelslist.every(char => char === 'a' || char === 'e' || char === 'i' || char === 'o' || char === 'u') 

console.log(result);

// Javascript engine : which is used to convert js code into machine code

// its responsible for converting the code into something that a computer can understand

//js engine are found in every brouser and are usually devloped by the browsers vendor

// the most popular js engines are v8, spidermonkey and chakra.

//node js is not a serverr using of express js we can make server express is a framework and node is server enviroment